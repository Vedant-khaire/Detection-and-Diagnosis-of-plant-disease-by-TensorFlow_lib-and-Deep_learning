<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Plant Disease Detection</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            overflow: hidden;
            background-color: rgb(255, 255, 255);
        }

        .background-image {
            background-image: url('');
            background-size: cover;
            background-position: center;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
        }

        .content {
            position: relative;
            z-index: 1;
            max-width: 600px;
            margin: 20px auto;
            text-align: center;
            background-color: #f0f0f0;
            padding: 20px;
            border-radius: 20px;
            box-shadow: 10px 10px 15px #d9d9d9, -10px -10px 15px #ffffff; /* Neumorphic box shadow */
        }

        h1 {
            color: #000;
            font-family: 'Times New Roman', Times, serif;
            margin-top: 0;
            margin-bottom: 20px;
        }

        form {
            text-align: center;
        }

        label {
            margin-bottom: 10px;
            display: block;
            text-align: left;
        }

        input[type="file"] {
            padding: 10px;
            margin-bottom: 20px;
            width: 100%;
        }

        button {
            background-color: #4caf50;
            color: #fff;
            padding: 10px 20px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-size: 16px;
            transition: background-color 0.3s ease;
        }

        button:hover {
            background-color: #45a049;
            animation: shake 0.5s;
        }

        @keyframes shake {
            0%, 100% {
                transform: translateX(0);
            }
            25%, 75% {
                transform: translateX(-5px);
            }
            50% {
                transform: translateX(5px);
            }
        }
    </style>
</head>
<body>

    <div class="background-image"></div>

    <div class="content">
        <h1>Plant Disease Detection</h1>

        <form action="/result" method="post" enctype="multipart/form-data" onsubmit="return validateForm()">
            <label for="image">Choose an image:</label>
            <input type="file" name="image" accept="image/*" required>
            <br>
            <button type="submit" id="detectButton">Detect Disease</button>
        </form>
    </div>

    <script>
       
    function validateForm() {
        var fileInput = document.querySelector('input[type="file"]');
        var allowedExtensions = /(\.jpg|\.jpeg|\.png)$/i;

        if (!fileInput.files.length) {
            alert('Please choose an image before detecting disease.');
            return false;
        }

        // Check if the file extension is allowed
        if (!allowedExtensions.exec(fileInput.value)) {
            alert('Invalid file type. Please select a valid image file (JPEG or PNG).');
            fileInput.value = ''; // Clear the file input
            return false;
        }

        return true;
    }
</script>


</body>
</html>
